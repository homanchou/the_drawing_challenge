<h1>New Admin Page</h1>

<%= form_for(@page, url: admin_manage_pages_path) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_manage_pages_path %>
