
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :slug %> (part of url)<br>
    <%= f.text_area :slug %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title, style: 'width:800px' %>
  </div>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, style: 'width:800px; height:400px' %>
  </div>

