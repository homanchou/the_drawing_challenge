<h1>Editing Admin Page</h1>

<%= form_for(@page, url: admin_manage_page_path(@page), method: :patch) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_manage_pages_path %>