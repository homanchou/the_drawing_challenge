<h1>Editing Admin Challenge</h1>

<%= form_for(@challenge, url: admin_manage_challenge_path(@challenge), method: :patch) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_manage_challenges_path %>