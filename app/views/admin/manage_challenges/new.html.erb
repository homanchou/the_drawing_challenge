<h1>New Admin Challenge</h1>

<%= form_for(@challenge, url: admin_manage_challenges_path) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_manage_challenges_path %>
